# Guía FIT en Flutter

Aplicación Dart con proyectos Android, iOS y web. Comparte las cuentas y el directorio mediante la API de Render, que guarda los datos en PostgreSQL de Aiven.

Consulta `../docs/FLUTTER.md`. Para explorar sin servidor: `flutter pub get` y `flutter run`; después pulsa **Explorar demostración**.
